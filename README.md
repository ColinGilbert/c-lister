Tiny little utility written to learn Boost and to help in writing makefiles.

Lists .c, .cc, .cpp files within a directory, recursively. 

Compile with:


```

g++ clister.cpp -o clister -lboost_system -lboost_filesystem


```

MIT-licensed.
